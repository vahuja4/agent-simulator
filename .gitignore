# Secrets — API keys live here; never commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.scratch/

# Claude Code worktrees (nested checkouts created by EnterWorktree)
.claude/worktrees/

# Journey harness development Runs (scripts/journey_harness.py run). A
# development Run is not evidence. Spot-check records (journey_spot_checks/) are
# evidence and are NOT ignored.
journey_runs/
